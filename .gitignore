.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.DS_Store

# hbdl runtime data (config/session/state live under the OS config dir, not
# the repo, but keep these ignored in case someone points --dest here)
/HumbleLibrary/
*.sqlite
session.json
HumbleBundleDownloaderNG.code-workspace

# docker-compose.yml's local volume mounts (real session cookie, config.toml,
# state db, Chromium/NSS profile data -- never belongs in the repo)
/hbdl-config/
/hbdl-data/
